image: node:latest

before_script:
  - npm install

stages:
  - pages

pages:
  stage: pages
  cache:
    paths:
    - node_modules/
  script:
  - npm run generate
  artifacts:
    paths:
    - dist
  only:
  - master
