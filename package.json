{
  "name": "wssmm",
  "version": "1.0.0",
  "description": "wssmm",
  "author": "lowrider-fn",
  "private": true,
  "scripts": {
    "eslint": "eslint --fix --ext .js,.ts,.tsx,.vue .",
    "stylelint": "stylelint **/*.{scss,vue} --fix",
    "stop": "docker container stop $(docker container ls -aq)",
    "rm:cont": "docker container rm $(docker container ls -aq)",
    "rm:img": "docker image prune -a",
    "rm:vol": "docker volume prune",
    "rm": "docker system prune",
    "up": "docker-compose up",
    "up:build": "docker-compose up --build"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^6.8.0",
    "eslint-config-airbnb-base": "^13.2.0",
    "eslint-plugin-import": "^2.20.1",
    "eslint-plugin-vue": "^5.2.3",
    "husky": "^4.2.3",
    "lint-staged": "^10.1.1",
    "stylelint": "^11.1.1",
    "stylelint-config-rational-order": "^0.1.2",
    "stylelint-config-recommended-scss": "^4.2.0",
    "stylelint-order": "^4.0.0",
    "stylelint-scss": "^3.16.0"
  }
}
